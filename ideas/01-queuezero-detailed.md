# QueueZero - AI-оркестратор очередей в банковских отделениях

## 🎯 Executive Summary

**Проблема**: 70% банковских операций в Узбекистане происходят офлайн. Клиенты тратят 40-90 минут в очередях. Банки теряют 30% потенциальных операций из-за того, что люди уходят, не дождавшись.

**Решение**: AI-система для прогнозирования загрузки отделений, умной маршрутизации клиентов и оптимизации расписания сотрудников.

**Результат**: 
- 50% сокращение времени ожидания
- 25% увеличение throughput операций
- 30% снижение потерянных клиентов

---

## 🔍 Deep Dive: Problem Analysis

### Текущая ситуация
- **Офлайн-зависимость**: Несмотря на рост digital banking, большинство операций требуют посещения отделения (открытие счетов, сложные переводы, кредитные консультации)
- **Неравномерная загрузка**: Пики в начале/конце месяца (зарплаты, платежи), праздничные дни, утренние часы
- **Примитивные очереди**: Электронные системы работают как счётчики, не учитывают сложность операций
- **Нет предсказуемости**: Клиенты не знают, когда придти, чтобы не ждать

### Потери для банка
- **Упущенные комиссии**: Каждый ушедший клиент = потенциальная операция
- **NPS падает**: Недовольные клиенты переходят к конкурентам
- **Неэффективность ресурсов**: Перегруженные сотрудники в пики, простой в другое время
- **Операционные затраты**: Переработки, стресс персонала, текучка кадров

---

## 💡 Solution Architecture

### Компоненты системы

#### 1. Predictive Load Forecasting
**Задача**: Предсказать количество клиентов и операций на следующие 7 дней.

**Данные**:
- Исторические данные о посещениях (timestamp, тип операции, длительность)
- Календарь (выходные, праздники, зарплатные дни)
- Внешние факторы (погода, местные события)

**Модель**: 
```python
# Time series forecasting
from lightgbm import LGBMRegressor
import pandas as pd

features = [
    'hour', 'day_of_week', 'day_of_month',
    'is_salary_day', 'is_holiday',
    'prev_day_load', 'prev_week_same_day_load',
    'rolling_7d_avg', 'rolling_30d_avg'
]

model = LGBMRegressor(
    n_estimators=1000,
    learning_rate=0.05,
    max_depth=7
)
```

**Output**: Прогноз на каждый час на 7 дней вперед (количество клиентов + типы операций)

---

#### 2. Intelligent Routing
**Задача**: Распределить клиентов по операционистам с учётом сложности операций.

**Классификация операций**:
- **Простые** (5-10 мин): Снятие/внесение наличных, простой перевод
- **Средние** (15-30 мин): Открытие депозита, заказ карты
- **Сложные** (30-60 мин): Открытие счёта, ипотечная консультация

**NLP для pre-classification**:
```python
# Клиент пишет в Telegram: "Хочу открыть депозит"
# LLM классифицирует запрос

from openai import OpenAI

prompt = f"""
Classify bank operation into: simple, medium, complex.
Client request: {user_message}

Operations:
- simple: cash withdrawal, basic transfer (5-10 min)
- medium: deposit opening, card order (15-30 min)  
- complex: account opening, loan consultation (30-60 min)

Return JSON: {{"type": "...", "estimated_time": ...}}
"""

classification = client.chat.completions.create(
    model="gpt-4o-mini",
    messages=[{"role": "user", "content": prompt}]
)
```

**Routing logic**:
- Простые операции → "быстрые" окна (designated fast lanes)
- Средние/сложные → опытные операционисты
- Real-time балансировка: если окно освободилось, перенаправить следующего

---

#### 3. Pre-Registration System
**Интерфейс**: Telegram Bot

**Флоу**:
```
1. Клиент открывает бота
2. Выбирает отделение
3. Выбирает тип операции
4. Видит прогноз ожидания на разное время
5. Бронирует временной слот
6. Получает QR-код и напоминание за 10 минут
7. В отделении: сканирует QR → становится в приоритетную очередь
```

**Преимущества**:
- Клиенты распределяются равномерно в течение дня
- Банк может предложить скидки на "непопулярные" часы
- Нет-show tracking: если клиент не пришёл, слот освобождается

---

#### 4. Staff Scheduling Optimization
**Задача**: Автоматически составить график смен на основе прогноза нагрузки.

**Input**:
- Прогноз загрузки на следующую неделю
- Доступность сотрудников
- Навыки (кто может обрабатывать сложные операции)
- Трудовое законодательство (макс. часы, перерывы)

**Optimization**:
```python
from pulp import *

# Decision variable: сотрудник i работает в час j
x = LpVariable.dicts("schedule", 
    [(staff, hour) for staff in staff_list for hour in hours],
    cat='Binary'
)

# Objective: минимизировать недогруз + перегруз
prob = LpProblem("Staff_Scheduling", LpMinimize)

prob += lpSum([
    abs(predicted_load[h] - lpSum([x[s,h] for s in staff_list]))
    for h in hours
])

# Constraints
for s in staff_list:
    # Макс 8 часов в день
    prob += lpSum([x[s,h] for h in hours]) <= 8
    
    # Минимум 1 час перерыв
    # Навыки: сложные операции только опытным
    # etc.
```

**Output**: Оптимальный график смен на неделю

---

## 🛠️ Technical Implementation

### Tech Stack

**Backend**:
- **FastAPI** (Python 3.11+) - REST API для всех компонентов
- **PostgreSQL** - хранение исторических данных, расписания
- **Redis** - real-time состояние очередей, кеш прогнозов
- **Celery** - фоновые задачи (обучение моделей, рассылка уведомлений)

**ML/AI**:
- **LightGBM** - forecasting загрузки
- **OpenAI GPT-4o-mini** - классификация запросов клиентов
- **Scikit-learn** - preprocessing, feature engineering
- **MLflow** - tracking экспериментов

**Frontend**:
- **Telegram Bot API** (python-telegram-bot) - клиентский интерфейс
- **React + TailwindCSS** - admin dashboard для менеджеров отделений
- **Recharts** - визуализация прогнозов и метрик

**Infrastructure**:
- **Docker + Docker Compose** - контейнеризация
- **Nginx** - reverse proxy
- **GitHub Actions** - CI/CD
- **VPS (Uzinfocom datacenter)** - compliance with local regulations

---

### System Architecture

```
┌─────────────────┐
│  Telegram Bot   │  ← Клиенты бронируют слоты
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   FastAPI       │  ← REST API
│   - /forecast   │
│   - /book       │
│   - /checkin    │
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌────────┐ ┌──────────┐
│ Redis  │ │PostgreSQL│
│(queue) │ │(history) │
└────────┘ └──────────┘
         │
         ▼
┌─────────────────┐
│ ML Pipeline     │
│ - Feature eng   │
│ - Model train   │
│ - Prediction    │
└─────────────────┘
         │
         ▼
┌─────────────────┐
│ Admin Dashboard │  ← Менеджеры отделений
│ - Load forecast │
│ - Queue monitor │
│ - Staff schedule│
└─────────────────┘
```

---

## 🚀 MVP Roadmap (2 недели)

### Week 1: Core Infrastructure
**Days 1-2**: Setup & Data
- Docker environment setup
- PostgreSQL schema design
- Mock historical data generation (если нет реальных данных)
- Redis setup for queue state

**Days 3-4**: Forecasting Model
- Feature engineering для time-series
- LightGBM model training
- Simple forecasting API endpoint
- Basic validation (RMSE, MAE)

**Days 5-7**: Telegram Bot
- Bot registration, basic commands
- `/start` - выбор отделения
- `/book` - бронирование слота
- Простая визуализация: "Сейчас очередь 30 мин, через 2 часа - 10 мин"
- QR code generation для check-in

### Week 2: Integration & Polish
**Days 8-9**: Routing Logic
- Classification API (GPT-4o-mini для типа операции)
- Routing algorithm: простые → fast lane, сложные → эксперты
- Integration с состоянием очереди в Redis

**Days 10-11**: Admin Dashboard
- React app setup
- Forecast visualization (линейный график на 7 дней)
- Real-time queue monitor (кто в очереди, время ожидания)
- Simple staff schedule display

**Days 12-13**: Testing & Demo Prep
- End-to-end testing флоу: бронирование → check-in → обработка
- Load testing (simulate 100 users booking simultaneously)
- Demo script подготовка
- Metrics dashboard для презентации

**Day 14**: Buffer & Documentation
- README для jury
- API documentation (Swagger)
- Deployment на VPS
- Video demo recording

---

## 📊 Success Metrics

### Для клиентов:
- **Average wait time** ↓ 50% (с 45 минут до 22 минут)
- **NPS** ↑ (измерить до/после через опрос в Telegram)
- **No-show rate**: tracking, сколько бронирований не используются

### Для банка:
- **Throughput** ↑ 25% (больше операций за день)
- **Lost customers** ↓ 30% (меньше уходят, не дождавшись)
- **Staff utilization** ↑ (меньше idle time, равномерная нагрузка)
- **Operational cost per operation** ↓

### Технические метрики:
- **Forecast accuracy**: MAPE < 15% для прогноза на 24 часа
- **Bot response time**: < 2 секунды для любой команды
- **System uptime**: 99.5% (критичная система)

---

## 💰 Business Model

### Монетизация для банков:
1. **Setup fee**: $5,000-10,000 за внедрение в 1 отделение
2. **SaaS subscription**: $500/отделение/месяц
3. **Revenue share**: 5% от увеличения комиссионных доходов (measurable через A/B test)

### Scaling plan:
- **Phase 1**: Agrobank (5-10 пилотных отделений)
- **Phase 2**: Rollout на всю сеть Agrobank (100+ отделений)
- **Phase 3**: White-label для других банков ЦА (Uzum Bank, Ipak Yuli, etc.)
- **Phase 4**: Expansion на другие отрасли (госуслуги, поликлиники, визовые центры)

### Market size (Узбекистан):
- 30+ банков × 50 отделений в среднем = 1,500 отделений
- $500/месяц × 1,500 = $750K MRR потенциал
- Реалистичный захват 10% за 2 года = $75K MRR

---

## 🚧 Risks & Mitigation

### Технические риски:
**Risk**: Плохое качество прогнозов из-за недостатка данных  
**Mitigation**: Начать с simple baseline (moving average), постепенно улучшать; использовать синтетические данные для MVP

**Risk**: Интеграция с внутренними системами банка может быть сложной  
**Mitigation**: MVP работает standalone (manual input в dashboard); integration на Phase 2

### Операционные риски:
**Risk**: Сопротивление сотрудников (боятся мониторинга эффективности)  
**Mitigation**: Позиционировать как инструмент помощи, а не контроля; показать, как это снижает стресс

**Risk**: Клиенты не будут использовать бронирование  
**Mitigation**: Gamification (бонусы за использование бота), приоритетная очередь для забронировавших

### Рыночные риски:
**Risk**: TBC или другой крупный игрок может скопировать  
**Mitigation**: First-mover advantage, быстрое масштабирование, эксклюзивные данные

---

## 🎯 Why This Will Win the Hackathon

1. **Immediate impact**: Решает реальную, ежедневную боль миллионов людей
2. **Measurable ROI**: Можно показать конкретные цифры уже через месяц пилота
3. **Scalable**: Не ограничено банками, можно масштабировать на другие индустрии
4. **Technical excellence**: Комбинация ML forecasting + NLP + optimization
5. **Demo-friendly**: Легко показать в Telegram, понятно jury и аудитории

---

## 📚 Appendix: Sample Code Snippets

### Telegram Bot (booking flow)
```python
from telegram import Update, InlineKeyboardButton, InlineKeyboardMarkup
from telegram.ext import Application, CommandHandler, CallbackQueryHandler

async def book_slot(update: Update, context):
    # Получить прогноз загрузки на сегодня
    forecast = get_load_forecast(branch_id=1, date='2025-11-24')
    
    # Показать клиенту опции
    keyboard = []
    for hour in range(9, 18):
        wait_time = forecast[hour]['predicted_wait_minutes']
        keyboard.append([
            InlineKeyboardButton(
                f"{hour}:00 - Ожидание ~{wait_time} мин",
                callback_data=f"book_{hour}"
            )
        ])
    
    reply_markup = InlineKeyboardMarkup(keyboard)
    await update.message.reply_text(
        "Выберите удобное время:",
        reply_markup=reply_markup
    )
```

### Forecasting Model (training)
```python
import pandas as pd
from lightgbm import LGBMRegressor
from sklearn.model_selection import TimeSeriesSplit

# Load historical data
df = pd.read_sql("SELECT * FROM branch_visits", conn)

# Feature engineering
df['hour'] = df['timestamp'].dt.hour
df['day_of_week'] = df['timestamp'].dt.dayofweek
df['is_salary_day'] = df['day_of_month'].isin([1, 10, 20])
df['lag_1d'] = df['num_visitors'].shift(24)
df['rolling_7d'] = df['num_visitors'].rolling(24*7).mean()

X = df[features]
y = df['num_visitors']

# Time series cross-validation
tscv = TimeSeriesSplit(n_splits=5)

model = LGBMRegressor(n_estimators=1000, learning_rate=0.05)
model.fit(X, y)

# Save model
import joblib
joblib.dump(model, 'forecast_model.pkl')
```

---

**Ready to build? Let's go! 🚀**
